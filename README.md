# ExpenseTracker

Desktop Expense Management System built with **Python (Tkinter)**, **MySQL** and **Matplotlib**.  
Lightweight, offline-first GUI app to add, filter, visualize and manage daily expenses.

---

## 🔍 Features
- Add / delete expense records (category, amount, date, description).  
- Filter by date range and category.  
- Monthly totals (bar chart) and category breakdown (pie chart) using Matplotlib.  
- MySQL backend for persistent storage.  
- Simple, clean Tkinter UI — runs on Windows / macOS / Linux (with Python + MySQL).

---

## 🧰 Tech stack
- Python 3.8+  
- Tkinter (GUI)  
- MySQL (database)  
- `mysql-connector-python` (DB connector)  
- Matplotlib (charts)

---
